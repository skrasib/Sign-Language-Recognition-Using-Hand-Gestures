# Adaptive Gesture Lab

An isolated frontend prototype for evaluating a new visual direction. It does not connect to the existing Tkinter application or alter its recognition logic.

Run locally with:

```bash
npm run dev
```

Then open the local URL shown in the terminal.

The prototype includes interactive Live, Teach, Library, Motion, and Settings views. Its controls simulate interface state only.
